<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:foreground="?attr/selectableItemBackground"
    android:padding="12dp">

    <!-- Top row: selection icon + title + badge -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical">

        <!-- Selection icon (checkbox style) -->
        <ImageView
            android:id="@+id/selection_icon"
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:layout_marginEnd="8dp"
            android:visibility="gone"
            android:contentDescription="@string/selection_icon_desc"
            android:src="@drawable/ic_check_box_outline_blank_24" />

        <!-- Title text -->
        <TextView
            android:id="@+id/completed_title"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="Title"
            android:textStyle="bold"
            android:textSize="16sp" />

        <!-- Badge -->
        <TextView
            android:id="@+id/completed_badge"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:paddingLeft="10dp"
            android:paddingRight="10dp"
            android:paddingTop="4dp"
            android:paddingBottom="4dp"
            android:text="SESSION"
            android:textSize="12sp"
            android:textColor="@android:color/white"
            android:background="@drawable/bg_badge_round" />
    </LinearLayout>

    <!-- Subtitle (location, timestamp, etc.) -->
    <TextView
        android:id="@+id/completed_subtitle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Subtitle"
        android:textSize="14sp"
        android:alpha="0.8"
        android:paddingTop="4dp" />

</LinearLayout>
